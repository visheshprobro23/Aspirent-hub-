import {NextResponse} from 'next/server';import bcrypt from 'bcryptjs';import {read} from '../../store';export async function POST(r:Request){const p=await r.json();const u=read().find(x=>x.email===String(p.email).toLowerCase());if(!u||!(await bcrypt.compare(String(p.password),u.password)))return NextResponse.json({error:'Invalid credentials'},{status:401});const out=NextResponse.json({user:{id:u.id,name:u.name,email:u.email}});out.cookies.set('ea_user',u.id,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:2592000});return out}
