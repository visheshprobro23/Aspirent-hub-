import {NextResponse} from 'next/server';import bcrypt from 'bcryptjs';import {read,write,id} from '../../store';export async function POST(r:Request){const p=await r.json();const db=read();if(db.some(u=>u.email===String(p.email).toLowerCase()))return NextResponse.json({error:'Email already registered'},{status:409});const u={id:id(),name:String(p.name),email:String(p.email).toLowerCase(),password:await bcrypt.hash(String(p.password),12),xp:0,level:1,streak:0,theme:'dark',tasks:[],goals:[],sessions:[],tests:[],messages:[]};db.push(u);write(db);const out=NextResponse.json({user:{id:u.id,name:u.name,email:u.email}});out.cookies.set('ea_user',u.id,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:2592000});return out}
