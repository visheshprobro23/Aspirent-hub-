'use client';import {useEffect,useState} from 'react';import Link from 'next/link';export default function Home(){const [u,setU]=useState<any>();useEffect(()=>{fetch('/api/me').then(r=>r.ok?r.json():null).then(x=>setU(x?.user))},[]);return <main className="auth"><section className="glass authbox center"><div style={{fontSize:52}}>🎯</div><h1>Exam Aspirant</h1><p className="muted">Your exam goal. Your daily mission. Your next step.</p>{u?<Link className="btn" href="/dashboard">Continue as {u.name}</Link>:<div className="row" style={{justifyContent:'center'}}><Link className="btn" href="/register">Let's Study</Link><Link className="btn secondary" href="/login">Login</Link></div>}</section></main>}
