import fs from 'fs';import path from 'path';export type User={id:string,name:string,email:string,password:string,exam?:string,examDate?:string,target?:number,xp:number,level:number,streak:number,theme:string,tasks:any[],goals:any[],sessions:any[],tests:any[],messages:any[]};const file=path.join(process.cwd(),'data.json');export function read():User[]{try{return JSON.parse(fs.readFileSync(file,'utf8'))}catch{return []}}export function write(x:User[]){fs.writeFileSync(file,JSON.stringify(x,null,2))}export function id(){return crypto.randomUUID()}export function xpLevel(xp:number){return Math.floor(Math.sqrt(xp/100))+1}
